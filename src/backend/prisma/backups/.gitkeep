# V1 backup files are generated by migrate-v1-to-v2.js
# They are excluded from version control to avoid committing potentially sensitive data.
# The backups directory itself is tracked so the path exists on fresh clones.
