[build]
builder = "nixpacks"
buildCommand = "npm run prisma:deploy"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
